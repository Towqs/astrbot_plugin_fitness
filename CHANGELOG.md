# 更新日志

## v2.0.11

### 新功能
- 新增动作视频引导：`今日计划` 会为训练动作发送 B 站动作教学分享卡片，可通过 `video_guide_enabled` 关闭。
- 新增用户自定义训练计划命令：`安排今天训练`、`安排明天训练`、`安排本周训练`、`清空本周训练`。
- 新增 `custom_plan_enabled` 配置，可统一开关用户自定义计划能力。

### 优化
- 周期训练模板结合性别、目标、器材生成更清晰的训练分化，减少任务雷同。
- 训练计划生成提示词补充“用户偏好优先、男/女只作默认侧重点”的规则。
- 帮助与 README 主推自然语言改计划，显式安排命令仅作为兜底入口保留。

## v2.0.10

### 新功能
- 新增 `健身帮助` 使用说明入口，群聊中艾特机器人发送 `健身帮助` 即可查看核心用法和常用命令。
- 管理员查询档案支持私聊操作，可通过附带群号查询指定群内用户档案或全员档案。

### 优化
- 帮助入口只保留 `健身帮助`，避免与其他插件的“菜单/说明/帮助”类提示词重复触发。

## v2.0.9

### Bug 修复
- 修复定时提醒重复发送：插件热重载时旧的 APScheduler 调度器未关闭，新调度器同时启动导致所有定时任务（晨间推送、打卡提醒等）触发两次
- 新增 `terminate()` 生命周期方法，插件卸载/停用时正确关闭调度器
- `ScheduledReminder.start()` 增加防御性检查，重复调用时先关闭已有调度器

## v2.0.8

### 新功能
- 晨间任务推送：每天早上（默认 8:00）自动艾特用户，推送今日训练任务（含具体动作、组数次数），休息日也会温馨提醒
- 训练前提醒：在用户偏好训练时间前 30 分钟自动提醒准备训练，已打卡则跳过
- 两种提醒均支持 AI 个性化生成（走低成本模型）和纯模板模式
- 新增 3 个配置项：`morning_briefing_enabled`、`morning_briefing_time`、`pre_workout_reminder_enabled`

## v2.0.7

### 新功能
- 训练计划动作模板库：`generate_cycle()` 和 `generate_deload_week()` 现在根据用户器材条件（健身房/家庭哑铃/纯徒手）生成具体动作、组数和次数，而非仅显示"胸/三头"等模糊描述
- 建档会话 TTL 超时机制：私聊建档会话 30 分钟未完成自动清理，防止内存泄漏

### 优化
- 提取 `get_checkin_by_date()` 公共函数到 `database.py`，替换 `tool_makeup_checkin()` 和 `cmd_makeup()` 中的重复内联 SQL
- `roll_random_event()` 添加注释说明单事件返回行为，防止后续误改

## v2.0.6

### 新功能
- 新增5个智能操作工具，LLM 可根据用户口语化请求自动调用：
  - `get_weekly_plans`: 查看未来N天训练计划（"这周练什么""看看我的计划"）
  - `delete_training_plans`: 删除指定日期范围的计划（"重新安排这周训练"）
  - `reset_quest`: 重置闯关任务进度或切换任务（"换个闯关任务""重新开始闯关"）
  - `get_full_analysis`: 获取完整综合分析数据（"帮我分析一下""我现在什么情况"）
  - `delete_profile`: 删除用户所有数据并支持重新建档（"我要重新建档"）
- 系统提示词新增智能操作工具说明和"重新安排计划"标准流程指引

### 修复
- 修复 database.py 中 `delete_plans_in_range` 和 `delete_profile` 函数重复定义的问题

## v2.0.5

### Bug 修复
- 修复所有 @llm_tool 工具返回"没有返回值"的严重 bug：将全部16个 LLM 工具方法从 `yield event.plain_result()` 生成器模式改为 `return` 协程模式
- 根因：AstrBot v4.22.0 的 tool executor 对 `yield event.plain_result()` 返回的 `MessageEventResult` 会走"直接发送给用户"分支，导致 tool runner 收到 `None`，无法将工具结果传回 LLM
- 改为 `return "字符串"` 后，executor 走协程分支，正确包装为 `CallToolResult` 返回给 LLM
- 此 bug 导致 LLM 无法获取工具返回数据（档案、计划、打卡结果等），引发训练计划目标不明确、数据展示异常等连锁问题

## v2.0.4

### 新功能
- 私聊建档：「健身注册」改为通过私聊分步采集隐私数据（身高体重等），保护用户隐私
- 建档完成后自动在群里通知 + 设置群头衔
- 私聊发送失败时自动回退到群内建档模式
- 新人进群欢迎消息更新为引导私聊建档

## v2.0.3

### Bug 修复
- 修复打卡提醒重复发送：添加并发锁 + `max_instances=1` + 提前标记已提醒，防止同一用户被重复提醒
- 修复数据泄漏：系统提示中明确禁止 AI 直接展示工具返回的 JSON 原始数据，必须用自然语言转述

### 新功能
- 建档完成后自动生成4周训练周期计划，用户无需手动触发

## v2.0.2

### 新功能
- 主动回复：教练以可配置概率主动回复群聊消息，概率可在配置中调整（默认关闭，建议5-15%）

## v2.0.1

### Bug 修复
- 修复 `update_status` 更新其他字段时会意外清除补剂状态的问题
- 修复 `create_profile` 分步建档时补剂状态被覆盖的问题
- 修复更新体重时未写入体重记录表，导致进步报告体重趋势为空
- 修复补卡不推进闯关进度的问题
- 修复补卡不触发成就检查的问题
- 修复饮食打卡未检查用户是否已建档的问题
- 修复成就经验导致升级时不更新群头衔的问题
- 修复训练周期 `current_week` 永远停在第1周不自动推进的问题
- 修正 README 中补卡描述与实际行为不一致（仅支持补前一天）

## v2.0.0

### 新功能
- 周期化训练（4-8周，渐进超负荷 + 去负荷周）
- 疲劳度评估（打卡时自动评估，高疲劳触发建议）
- 饮食打卡（自然语言记录，AI 估算热量和蛋白质）
- 成就系统（12种成就，多种触发条件）
- 进步检测（自动对比近期与历史数据）
- 用户画像（训练偏好、恢复能力、疲劳趋势）
- 群周报（每周日自动发送排行榜和统计）
- 周六体重反馈（定时询问体重变化）
- 补卡机制（补前一天的卡，经验减半）
- 渐进式信息收集（日常对话中自然收集用户信息）
- AI 综合分析（自动生成训练方向建议）
- 双模型支持（高成本对话 + 低成本提醒）
- 5种教练人格可选
